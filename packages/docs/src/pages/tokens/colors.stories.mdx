import { Meta } from '@storybook/addon-docs'
import { ColorsGrid } from '../../components/ColorsGrid'
import { colorsWhite } from '@capfds-ui/tokens'
import { colorsDark } from '@capfds-ui/tokens'

<Meta title="Tokens/Colors" />

# Colors White
Essas são as cores do tema claro utilizadas no Ignite UI.
<ColorsGrid colors={colorsWhite}/>

# Colors Dark
Essas são as cores do tema Escuro utilizadas no Ignite UI.
<ColorsGrid colors={colorsDark}/>